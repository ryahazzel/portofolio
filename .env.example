# Postgres connection string.
# In production this should point to the read-only "portfolio_reader" role
# (SELECT-only on portfolio_profile, portfolio_project, work_experience) —
# this app never writes to the database.
DATABASE_URL=
